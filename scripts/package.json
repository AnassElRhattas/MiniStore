{
  "name": "mini-store-scripts",
  "version": "1.0.0",
  "description": "Scripts for Mini Store Firebase setup",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "seed": "npm run build && node dist/seedData.js",
    "create-admin": "npm run build && node dist/createAdmin.js"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3"
  }
}
